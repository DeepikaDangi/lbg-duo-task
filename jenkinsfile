pipeline {
  agent any
  stages {
    stage('down') {
      steps {
        sh 'docker-compose down'
      }
    stage('build') {
      steps {
        sh 'docker-compose build'
      }
    }
    stage('run') {
      steps {
        sh 'docker-compose up -d'
      }
    }
  }
}
